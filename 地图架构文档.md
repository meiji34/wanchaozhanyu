# 地图模块接入核心 HUD

> 适用版本：Godot 4.7.1。旧版文档中的 `Node2D`、`Camera2D` 和 2D 地图描述已经失效；地图实现以 `MAP_AGENTS.md` 的 3D 约束为准。

## 当前结构

```text
MainHUD
└─ MapArea (Control)
   ├─ PlaceholderBackground (ColorRect, IGNORE)
   ├─ MapViewportContainer (SubViewportContainer, PASS)
   │  └─ MapViewport (SubViewport)
   │     └─ MapWorld (Node3D)
   ├─ PlaceholderLabel (Label, IGNORE)
   └─ MapUILayer (Control, IGNORE)
      └─ MapTools
         ├─ ZoomIn
         ├─ ZoomOut
         └─ ResetView
```

- 地图主场景固定为 `res://map/map_world.tscn`，根节点为 `MapWorld (Node3D)`。
- `MapArea` 负责安全加载、占位回退、输入桥接、地图工具和中立选择快照。
- `MapWorld` 负责地图渲染、相机、拾取、选中特效、Chunk 与实体，不访问 HUD 节点。
- `MapUILayer` 根节点必须保持 `MOUSE_FILTER_IGNORE`；只有真实按钮拦截自身矩形。

## UI 契约

`MapArea` 对 HUD 发出：

```gdscript
signal map_ready
signal map_load_failed(message: String)
signal selection_changed(selection: Dictionary)
signal selection_cleared
```

并提供 `zoom_in()`、`zoom_out()`、`reset_view()` 和 `get_map_world()`。地图场景缺失、无法读取或根节点类型错误时，组件显示“地图暂不可用”，发出 `map_load_failed`，但不会中断 HUD。

`selection_changed` 的城池快照包含：

```gdscript
{
    "kind": "city",
    "id": String,
    "tile_id": Vector2i,
    "name": String,
    "level": int,
    "faction": String,
}
```

格子快照包含：

```gdscript
{
    "kind": "tile",
    "tile_id": Vector2i,
    "terrain": String,
    "height": float,
    "has_road": bool,
    "can_build_city": bool,
}
```

这些字段只用于显示。城池经营、战斗、行军、资源和服务器状态仍由 UI、玩法与后端模块负责。

## 地图接口

`MapWorld` 保留原始中立信号：

```gdscript
signal tile_selected(tile_id: Vector2i)
signal city_selected(city_id: String, tile_id: Vector2i)
signal selection_cleared
```

并提供：

- `get_city_snapshot(city_id)`：转换城池显示数据。
- `get_tile_snapshot(tile_id)`：转换格子显示数据。
- `zoom_by_factor(factor, viewport_size)`：按实际视口缩放。
- `reset_view(viewport_size)`：回到地图中心、默认正交尺寸、北向与默认俯仰角，并清除选择。

未来单位选择使用独立 `unit_selected` 信号扩展，不改变现有城池和格子快照。

## 输入与布局

- 鼠标左键拖动平移，滚轮缩放，右键或中键拖动旋转和俯仰。
- 移动端单指拖动；双指缩放与整体拖动旋转/俯仰。
- 输入坐标由 `MapInputAnchor` 按容器和 `SubViewport` 实际尺寸换算，不使用固定设计分辨率。
- HUD 顶栏、任务抽屉、导航、弹窗与选择抽屉只拦截自身范围，其余区域继续操作地图。
- 目标视口包括 16:9、超宽横屏和移动设备安全区域。

## 联调验收

- `map_world.tscn` 可独立实例化并作为 `MapViewport` 子节点运行。
- 地图缺失时保留占位层，HUD 和导航继续可用。
- 地图工具按钮至少为 `56 × 56`，按钮外不吞地图输入。
- 城池和格子选择能打开正确上下文抽屉，取消选择立即关闭。
- 单指、双指和桌面调试输入均能到达地图模块。
- HUD 切出后地图随 `MapViewport` 释放，无重复信号或残留引用。
- 控制台不存在地图接入造成的未处理错误。
