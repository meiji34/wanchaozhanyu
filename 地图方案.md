# 地图方案

## 类型

Grid-based 3D World System（方格 3D 世界系统）

## 地图思想

2.5D：`Sprite3D`、轻量 3D 模型与正交摄像机。

- PNG 地表资源由 `Sprite3D` 或等价平面放置到 3D 格子。
- 数据层使用 Tile 数据，表现层使用 3D Tile Prefab，由地图管理器统一加载。

## 模型资源

- 模型使用 GLTF/GLB。
- 导入后优先包装或转换为 Godot 场景再实例化。

## 加载方案

动态加载与地图分块。远景首版使用低细节、降低密度或减少阴影，不使用高成本虚化。

```text
MapController
  -> 读取地图数据
  -> 判断摄像机位置
  -> 计算目标 Chunk
  -> 分帧加载或回收 Chunk
  -> 实例化 3D 资源
```

## 摄像机设计

玩家可自由平移、缩放、旋转与调整俯仰角。

```text
CameraRig
└─ Camera3D
```

## 地图点击

屏幕坐标 -> Camera3D 射线 -> 地面交点 -> 3D 格子 -> Tile ID / Entity ID。

详细参数、模块边界和验收要求以 `MAP_AGENTS.md` 为准。
