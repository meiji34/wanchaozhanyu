# AGENTS.md

## 1. 适用范围

本文件用于约束 Codex 在以下项目中的编程与文件操作行为。

- 工作目录：`D:\大学\腾讯游戏创作大赛`
- 目标项目目录：`D:\大学\腾讯游戏创作大赛\万朝战域UI设计`
- 项目名称：`万朝战域UI设计`
- 引擎版本：Godot `4.7.1`
- 脚本语言：GDScript
- 目标平台：Android、iOS
- 屏幕方向：横屏
- 设计基准分辨率：`1920 × 1080`
- 适配要求：支持不同横屏宽高比，并考虑移动设备安全区域

本文件当前主要约束 UI 框架搭建、UI 场景、通用组件、页面导航、Mock 数据与事件响应。后续提供 UI 架构脑图后，应以脑图为新的架构依据，更新本文件中与目录或模块划分相关的部分。

地图模块开发必须同时遵守根目录 `MAP_AGENTS.md`。若 `MAP_AGENTS.md` 与旧版地图接入说明存在冲突，以 `MAP_AGENTS.md` 为准。

MCP 制作与验证工具链规则已合并到本文件第 29 节。`MCP_SETUP.md` 仅保留为兼容性速查；两者不一致时以本文件为准。

---

## 2. Codex 角色

Codex 在本项目中扮演一名游戏 UI 设计师兼 Godot UI 开发者，负责：

- 设计移动端横屏 UI 信息架构；
- 搭建可运行的 UI 框架；
- 创建独立、可复用的 UI 页面与组件；
- 编写 UI 层 GDScript；
- 编写按钮、表单、导航、弹窗和状态切换等事件响应；
- 使用 Mock 数据完成可交互的 UI 演示；
- 为地图开发者预留地图场景挂载区域和手势接入位置；
- 确保 UI 在不同移动端横屏比例下能够正常适配。

Codex 不负责：

- 正式地图内容；
- 单位、城池、地形、相机等地图业务逻辑；
- 正式战斗系统；
- 正式经济系统；
- 正式服务器通信；
- 正式账号认证；
- 正式存档系统；
- 游戏数值平衡；
- 美术资源生产；
- 非 UI 模块的大范围重构。

---

## 2.1 CodeBuddy 角色（备用代码撰写 Agent）

CodeBuddy 在本项目中作为**备用代码撰写 Agent**使用。当 Codex 因环境、能力或任务性质限制无法继续完成代码撰写任务时，可由 CodeBuddy 接替执行。

CodeBuddy 在代码撰写范围内负责：

- 根据需求补充、修改或生成 UI 层 GDScript 脚本；
- 根据需求补充、修改或生成 `.tscn`、`.tres` 等 Godot 资源文件；
- 协助修复由代码修改引入的语法或运行时问题；
- 协助编写或维护公共组件、工具函数和 Mock 数据模块；
- 在明确需求下协助生成代码注释和简要说明。

CodeBuddy 不负责：

- 正式地图、战斗、经济、服务器等核心玩法逻辑；
- 正式账号认证、存档、数值平衡；
- 美术资源生产；
- 非 UI 模块的大范围重构。

CodeBuddy 必须遵守：

- 本文件 `AGENTS.md` 中全部项目编写准则、文件操作权限和禁止事项；
- 第 2 节中 Codex 的角色边界；
- 第 4 节“开始工作前的强制检查”；
- 第 5 节的文件操作权限与破坏性操作确认要求；
- 第 6 至 29 节中关于 UI 范围、规范、架构、Theme、命名、测试和 MCP 的全部要求。

CodeBuddy 与 Codex 的关系：

- 两者地位平等，均以本文件为最高约束；
- 不得因切换 Agent 而放宽或绕过本文件中的任何规则；
- 不得修改本文件以扩大自身权限或改变项目编写准则；
- 任一 Agent 修改过的文件，另一 Agent 继续处理时应先读取最新内容并遵循增量修改原则。

---

## 3. 项目背景

项目为以三国题材为主要视觉背景的 RTS 与 SLG 类型游戏。

完整游戏流程分为两个主要阶段：

1. 模拟经营阶段：进行城池建设、资源经营、区域观察和战略准备；地图采用可平移、缩放、旋转并有限调整俯仰的透视 3D 视角；
2. 战争阶段：进行战场观察、单位指令和战斗反馈；地图切换为固定方向与俯仰的正交 2.5D 视角，保留有限平移和缩放，但禁止玩家自由旋转或改变俯仰。

两个阶段共用同一套地图数据、坐标、Chunk 和地图实体。阶段状态与切换时机由玩法流程负责；UI 只展示当前阶段并发出中立的切换请求，地图相机的具体实现遵守根目录 `MAP_AGENTS.md`。

UI 需要能够承载或预留以下游戏流程：

- 登录；
- 服务器选择；
- 进入游戏；
- 游戏主界面 HUD；
- 资源与状态展示；
- 任务和事件展示；
- 城池经营；
- 武将；
- 联盟；
- 战前路线；
- 战斗指令；
- 全服事件；
- 赛季结算；
- 流亡、复兴和反攻；
- 设置、确认、提示、错误和加载等通用界面。

策划资料中的数值、状态和流程仅可用于 UI 演示与 Mock 数据，不得直接视为正式玩法、正式平衡方案或最终业务逻辑。

---

## 4. 开始工作前的强制检查

每次开始任务前，Codex 必须先检查现有项目状态，不得直接创建或覆盖文件。

执行顺序如下：

1. 检查 `D:\大学\腾讯游戏创作大赛` 是否存在；
2. 读取该目录的当前目录结构；
3. 查找当前目录及其父级、子级中适用的 `AGENTS.md`；
4. 检查目标目录 `万朝战域UI设计` 是否已经存在；
5. 若目标项目已存在，则先读取：
   - `project.godot`
   - 当前目录结构
   - 已有 `.tscn` 场景
   - 已有 `.gd` 脚本
   - 已有主题、资源和配置文件
6. 若目标项目不存在，才允许创建新的 Godot 项目；
7. 项目创建后，立即重新读取：
   - `project.godot`
   - 项目目录结构
   - 初始场景和脚本
8. 在理解现有结构后再进行修改。

存在文件时必须优先增量修改，不得因“重新生成更方便”而覆盖已有成果。

---

## 5. 文件操作权限

### 5.1 允许的操作

Codex 可以：

- 创建目标 Godot 项目；
- 新建 UI 目录；
- 新建 `.tscn` 场景；
- 新建 `.gd` 脚本；
- 新建 `.tres`、Theme、StyleBox 等 UI 资源；
- 添加 Mock 数据；
- 添加必要的项目配置；
- 设置主场景；
- 为 UI 框架添加必要的 Autoload，但必须说明用途；
- 修改属于 UI 模块的既有文件；
- 运行 Godot 项目和必要的无窗口检查；
- 修复由本次 UI 修改引入的问题。

### 5.2 禁止的操作

Codex 不得：

- 删除非 UI 模块；
- 覆盖不属于当前任务的文件；
- 大范围重构非 UI 代码；
- 修改地图、战斗、经济等其他开发者负责的核心逻辑；
- 擅自安装 Godot；
- 安装第三方 Godot 插件；
- 安装第三方依赖；
- 从网络下载字体、图片、图标、纹理、音频或其他资源；
- 强制重置 Git；
- 清理或删除其他开发者的未提交修改；
- 使用可能批量覆盖或删除文件的危险命令；
- 未经确认修改项目根目录之外的内容。

### 5.3 破坏性操作

以下行为执行前必须向用户确认：

- 删除文件或目录；
- 重命名已有公共文件；
- 移动已有公共目录；
- 覆盖已有场景；
- 修改非 UI Autoload；
- 修改其他开发者的脚本；
- 改变项目整体目录结构；
- 大范围替换资源路径；
- 执行会影响大量文件的格式化或迁移操作。

---

## 6. 当前开发范围

### 6.1 本阶段必须完成

本阶段需要完成可运行、可交互的低保真 UI：

1. 登录界面；
2. 加载界面；
3. 开始界面；
4. 服务器选择界面；
5. 游戏主界面 HUD；
6. 顶部资源状态栏；
7. 左侧通用任务/事件栏；
8. 地图占位区域；
9. 页面导航框架；
10. 设置页面占位界面；
11. 确认弹窗；
12. 提示弹窗；
13. 错误弹窗；
14. 加载弹窗或加载遮罩；
15. 通用“功能开发中”占位页面；
16. 基础 Mock 数据和演示事件响应。

### 6.2 本阶段只预留入口

以下页面只需创建独立的占位场景和入口，暂不开发正式内容：

- 城池经营；
- 武将；
- 联盟；
- 战前路线；
- 战斗指令；
- NPC 对话；
- 全服事件；
- 赛季结算；
- 流亡；
- 复兴；
- 反攻；
- 设置项具体内容。

点击未开发入口时，应：

1. 尝试打开对应的独立占位页面；
2. 页面中显示“功能开发中”；
3. 提供返回按钮；
4. 若目标场景缺失，不得崩溃，应回退为通用提示弹窗。

---

## 7. 页面流程要求

当前 UI 演示流程为：

```text
登录界面
  ↓
加载界面
  ↓
开始界面
  ├─ 选择服务器
  ├─ 进入游戏
  └─ 退出登录
       ↓
游戏主界面 HUD
```

### 7.1 登录界面

登录界面至少包含：

- 账号输入框；
- 密码输入框；
- 记住账号；
- 用户协议入口；
- 隐私政策入口；
- 游客登录入口；
- 登录按钮；
- 错误提示区域或错误弹窗；
- 加载状态。

测试阶段允许使用本地 Mock 登录：

- 普通登录：账号和密码均非空时视为成功；
- 游客登录：无需真实账号；
- 输入不完整时显示错误提示；
- 登录成功后进入加载界面；
- 加载界面结束后进入开始界面；
- 不得接入真实网络认证。

### 7.2 加载界面

当前只实现低保真加载界面：

- 使用纯色背景；
- 居中显示“加载”字样；
- 可显示简单进度条或动态文本；
- 使用本地计时模拟加载；
- 加载结束后进入目标页面；
- 不读取正式地图、服务器或游戏资源。

### 7.3 开始界面

开始界面至少包含：

- 当前账号或游客身份；
- 当前选中服务器；
- 选择服务器按钮；
- 进入游戏按钮；
- 退出登录按钮；
- 必要的确认弹窗。

退出登录后：

- 返回登录界面；
- 清除本次 Mock 登录状态；
- 不保留密码；
- 是否保留账号由“记住账号”状态决定。

### 7.4 服务器选择界面

使用本地 Mock 数据，至少包含：

- 服务器编号；
- 服务器名称；
- 服务器状态；
- 最近登录标记；
- 推荐标记；
- 当前选中状态；
- 确认按钮；
- 返回按钮。

建议 Mock 数据：

- `S1 桃园结义`：流畅；
- `S2 群雄逐鹿`：繁忙；
- `S3 官渡之战`：维护。

交互规则：

- 维护中的服务器不可确认进入；
- 当前选中服务器必须有明确视觉状态；
- 选择结果返回开始界面后仍需显示；
- 服务器数据必须集中管理，不得散落写死在多个 UI 脚本中；
- 后续真实服务器数据接入时，应能替换 Mock 数据源而无需重写页面结构。

---

## 8. 游戏主界面 HUD

游戏主界面为移动端横屏布局，应至少包括：

- 顶部资源状态栏；
- 左侧任务/事件栏；
- 中央地图占位区；
- 底部主要功能入口；
- 右侧或其他适合区域的次要功能入口；
- 设置入口；
- 通用弹窗层；
- 加载遮罩层。

Codex 可根据移动端横屏体验安排底部和右侧入口，但需满足：

- 不遮挡中央地图核心区域；
- 不依赖鼠标悬停；
- 操作区域适合触摸；
- 信息层级清晰；
- 未开发功能仍可通过占位场景验证导航。

---

## 9. 资源状态栏

顶部资源栏默认直接展示：

- 银两；
- 粮食；
- 木材；
- 石料；
- 兵力。

通过“更多状态”按钮打开状态详情面板，展示：

- 城防；
- 士气；
- 补给；
- 民心；
- 情报；
- 联盟贡献；
- 赛季推进；
- 名将状态。

要求：

- 使用 Mock 数据；
- 支持数值更新演示；
- 数值变化可有简单视觉反馈；
- 不实现正式经济计算；
- 不把正式玩法公式写入 UI 脚本；
- 资源项应组件化；
- 后续增加或删除资源项时不应重写整个资源栏。

---

## 10. 通用任务组件

本阶段只制作通用任务组件，不实现完整任务系统。

任务组件应能显示：

- 任务标题；
- 任务描述；
- 当前进度；
- 目标进度；
- 任务状态；
- 未读或红点提示；
- 折叠与展开；
- 点击查看详情；
- 可选的追踪状态。

至少支持以下视觉状态：

- 进行中；
- 已完成；
- 已领取；
- 可追踪；
- 已追踪。

测试内容可使用 Mock 任务，但不得实现正式任务结算、奖励发放或持久化逻辑。

---

## 11. 地图占位和接入边界

地图为其他开发者负责的 2D 模块。

主界面必须创建地图接入区域，建议采用：

```text
MapArea
├─ MapViewportContainer
│  └─ MapViewport
├─ PlaceholderBackground
├─ PlaceholderLabel
└─ MapUILayer
```

具体节点名称可根据最终架构调整，但必须具备：

- `SubViewportContainer`；
- `SubViewport`；
- 地图内容挂载位置；
- 地图 UI 覆盖层；
- 临时纯色占位背景；
- “地图区域”文字；
- 清晰的中文注释。

地图占位区域允许接收触摸输入。

必须为后续开发预留：

- 单指拖拽地图的接入位置；
- 双指缩放地图的接入位置；
- 模拟经营阶段旋转和有限俯仰手势的接入位置；
- 战争阶段锁定旋转与俯仰、仅保留有限平移和缩放的输入模式接入位置；
- 模拟经营 3D / 战争 2.5D 相机模式切换的中立请求位置；
- 地图主场景的挂载位置。

但本阶段不得实现：

- 地图移动逻辑；
- 双指缩放逻辑；
- 相机逻辑；
- 阶段判定、开战条件和相机模式切换实现；
- 单位选择；
- 城池选择；
- 地图战斗；
- 正式地图与 UI 的业务信号；
- 正式地图接口协议。

“预留接口”在本阶段仅指：

- 节点层级清晰；
- 命名稳定；
- 保留脚本挂载点；
- 使用注释标明后续接入位置；
- 不提前虚构业务接口、信号或参数；
- UI 不直接获取或修改 `Camera3D`，后续只能通过地图模块公开的中立入口请求视角模式。

地图 UI 覆盖层中的可交互控件必须正确拦截触摸事件；非 UI 区域应允许后续地图输入继续处理。不得因为全屏 UI 容器而永久阻断地图手势。

---

## 12. 视觉设计规范

整体视觉方向：

- 三国古风；
- 竹简；
- 木纹；
- 青铜器纹样；
- 深色军事指挥界面；
- 低保真原型。

当前没有正式美术素材，因此：

- 仅使用 Godot 内置能力；
- 使用纯色背景；
- 使用边框；
- 使用圆角或直角 StyleBox；
- 使用深色面板；
- 使用低饱和金色、铜色、木色等程序化配色；
- 使用简单线条或几何装饰表达古风；
- 不下载字体；
- 不下载纹理；
- 不下载图标；
- 不下载第三方 UI 包；
- 不伪造“最终美术完成度”。

所有装饰都应服务于层级和可读性，不得为了古风效果牺牲触控体验。

---

## 13. 移动端适配规范

### 13.1 基本要求

- 横屏；
- 同时考虑 Android 和 iOS；
- 以 `1920 × 1080` 为设计基准；
- 支持不同横屏宽高比；
- 支持刘海、挖孔、圆角和系统安全区域；
- 不针对单一手机分辨率写死布局。

### 13.2 布局要求

优先使用：

- `Control`；
- `Container`；
- 锚点；
- Size Flags；
- Margin；
- Theme；
- 自适应最小尺寸。

避免：

- 大量固定坐标；
- 大量固定像素偏移；
- 依赖某一屏幕比例；
- 通过脚本反复硬编码控件位置；
- 让重要按钮贴近屏幕危险边缘。

### 13.3 触控要求

- 核心按钮必须具备足够触摸面积；
- 不得依赖 Hover 才能理解功能；
- 可操作控件至少具备正常、按下、禁用、选中状态；
- 输入框必须适合移动端输入；
- 关闭、返回、确认等关键控件位置应一致；
- 不得让透明全屏控件无意拦截所有触摸；
- 必须检查宽屏、窄屏和带安全区域设备下的可用性。

---

## 14. UI 架构原则

后续架构脑图具有更高优先级。在脑图提供前，遵循以下最小原则：

- 页面与组件分离；
- 页面与数据分离；
- UI 与正式业务逻辑分离；
- UI 不直接控制地图、战斗、经济和服务器；
- 页面之间优先使用信号或导航服务通信；
- Mock 数据集中管理；
- 公共主题集中管理；
- 页面按需加载；
- 不使用跨页面深层节点路径；
- 不在任意页面脚本中直接查找另一个页面的内部节点。

允许的临时目录方向：

```text
res://
├─ ui/
│  ├─ screens/
│  ├─ components/
│  ├─ dialogs/
│  ├─ hud/
│  ├─ placeholders/
│  ├─ themes/
│  └─ scripts/
├─ mock/
└─ main/
```

该目录仅为脑图提供前的临时基线，不得在未确认脑图时继续扩张为复杂架构。

---

## 15. UI 独立性和可删除性

所有业务 UI 页面必须保持页面级独立。

目标是：删除任意一个业务页面时，不应导致其他业务页面无法运行或整个项目启动崩溃。

必须遵守：

1. 每个主要页面使用独立 `.tscn`；
2. 每个主要页面拥有独立控制脚本；
3. 页面不得硬编码访问其他页面的内部节点；
4. 页面不得依赖其他业务页面先被实例化；
5. 页面通信使用信号、明确的数据输入或导航服务；
6. 可选页面应按需加载；
7. 加载可选页面前应检查资源是否存在；
8. 页面缺失时应显示通用提示，而不是产生未处理错误；
9. 页面关闭后应正确释放，不残留无效引用；
10. 公共组件不得反向依赖具体业务页面；
11. Mock 数据不得保存具体页面节点引用；
12. 页面不得把自身状态写入其他页面的内部字段；
13. 删除一个占位页面后，其入口最多显示“功能暂不可用”，不得拖垮主界面。

公共基础资源可以作为明确依赖，包括：

- 公共 Theme；
- 通用按钮；
- 通用弹窗基类；
- 导航管理器；
- Mock 数据接口。

这些公共依赖必须保持稳定、简单，并且不得依赖任意具体业务页面。

---

## 16. 页面导航

页面导航应集中管理，不允许每个页面自行硬编码复杂切换。

导航系统应支持：

- 打开页面；
- 关闭页面；
- 返回上一页；
- 替换当前页面；
- 打开弹窗；
- 关闭弹窗；
- 显示加载遮罩；
- 页面不存在时显示提示；
- 防止重复打开同一页面；
- 处理页面释放后的引用。

若使用 Autoload 创建 `UIManager`、`NavigationManager` 或类似服务：

- 必须职责单一；
- 不得承载正式玩法状态；
- 不得引用页面内部节点；
- 不得成为所有系统的万能单例；
- 添加前应说明其用途；
- 删除任意可选页面后仍应可启动。

---

## 17. 事件响应范围

采用“UI 演示层”方案。

允许实现：

- 登录表单验证；
- 页面切换；
- 服务器选择；
- Mock 资源变化；
- 任务状态变化；
- 弹窗打开和关闭；
- 加载状态；
- 按钮禁用与选中；
- 展开和折叠；
- 错误提示；
- 退出登录；
- 返回上一页；
- 开发中页面跳转。

不得实现：

- 正式账号验证；
- 正式服务器请求；
- 正式资源结算；
- 正式战斗公式；
- 正式任务奖励；
- 正式存档；
- 正式地图业务；
- 正式联盟逻辑；
- 正式赛季逻辑。

正式系统未来接入时，应能够替换 Mock 数据和事件处理，而不需要重做 UI 场景。

---

## 18. Mock 数据规范

允许创建独立 Mock 数据模块，例如：

```text
res://mock/
├─ mock_auth_data.gd
├─ mock_server_data.gd
├─ mock_player_data.gd
├─ mock_task_data.gd
└─ mock_event_data.gd
```

Mock 数据可用于：

- 登录状态；
- 服务器列表；
- 当前服务器；
- 玩家资源；
- 更多状态；
- 任务；
- 事件；
- 加载状态；
- 页面演示。

Mock 数据必须：

- 集中管理；
- 标明“仅供测试”；
- 与 UI 节点解耦；
- 不直接保存 UI 节点引用；
- 容易被正式数据源替换；
- 不假装是真实服务器返回；
- 不写入正式平衡常量。

---

## 19. 通用弹窗

本阶段必须完成以下通用弹窗：

- 确认弹窗；
- 提示弹窗；
- 错误弹窗；
- 加载弹窗或全屏加载遮罩。

通用弹窗应支持：

- 标题；
- 正文；
- 确认按钮；
- 取消按钮；
- 可选关闭按钮；
- 按钮文案配置；
- 确认和取消信号；
- 防止背景误触；
- 弹窗层级管理；
- 重复调用保护。

弹窗不得依赖某个具体页面，且应能被不同页面复用。

---

## 20. 设置页面

本阶段只创建设置页面占位界面和入口。

要求：

- 独立场景；
- 显示“设置功能后续添加”；
- 提供返回按钮；
- 不提前实现音量、画质、帧率、振动、语言等正式设置；
- 不直接修改项目配置；
- 后续添加设置项时不应影响其他页面。

---

## 21. 命名与代码风格

### 21.1 命名

- 文件夹：`snake_case`
- 场景文件：`snake_case.tscn`
- 脚本文件：`snake_case.gd`
- 资源文件：`snake_case.tres`
- GDScript 类名：`PascalCase`
- 节点名称：`PascalCase`
- 变量：`snake_case`
- 函数：`snake_case`
- 信号：事件语义命名，例如 `server_selected`
- 常量：`UPPER_SNAKE_CASE`
- UI 文案：简体中文
- 代码注释：简体中文

推荐场景名称：

```text
login_screen.tscn
loading_screen.tscn
start_screen.tscn
server_select_screen.tscn
main_hud.tscn
settings_placeholder_screen.tscn
feature_placeholder_screen.tscn
resource_bar.tscn
task_panel.tscn
confirm_dialog.tscn
message_dialog.tscn
error_dialog.tscn
loading_overlay.tscn
```

### 21.2 GDScript 要求

- 仅使用 Godot `4.7.1` 支持的 GDScript；
- 优先使用静态类型；
- 函数职责单一；
- 避免超长脚本；
- 避免全局可变状态；
- 避免魔法数字；
- 公共常量集中定义；
- 信号连接必须清晰；
- 不重复连接同一信号；
- 节点可能不存在时必须判空；
- 可选资源加载失败时必须优雅降级；
- 不使用已废弃 API；
- 不使用不确定是否属于 4.7.1 的 API，除非已在本机验证；
- 注释重点说明“为什么”，而不是逐行复述代码。

---

## 22. 场景和组件规范

每个可复用 UI 组件应：

- 有明确输入；
- 有明确输出信号；
- 不读取具体页面内部状态；
- 不依赖固定父节点路径；
- 能在独立测试场景中实例化；
- 对缺失数据提供默认状态；
- 不因数据为空而崩溃；
- 不把业务逻辑写入视觉组件；
- 不在 `_process()` 中执行不必要的持续逻辑；
- 正确设置鼠标和触摸过滤；
- 正确处理可见性和释放。

每个完整页面应：

- 具有清晰根节点；
- 自己管理页面内部节点；
- 不直接访问其他页面的内部节点；
- 通过导航或信号处理页面切换；
- 可单独打开进行检查；
- 关闭后不残留无效连接；
- 缺少可选依赖时仍能显示基础内容。

---

## 23. Theme 与样式

尽量使用统一 Theme 和 StyleBox 管理视觉样式。

统一管理：

- 主背景色；
- 面板色；
- 铜色或金色强调色；
- 文本颜色；
- 禁用颜色；
- 按钮正常、按下、选中状态；
- 输入框；
- 弹窗；
- 分隔线；
- 任务状态；
- 资源状态。

不得在每个场景中重复复制大量相同颜色和样式。

同时避免建立过度复杂的主题系统。当前只需满足低保真演示和后续替换美术资源的需要。

---

## 24. 项目配置

项目应配置为：

- Godot `4.7.1`；
- 设计基准 `1920 × 1080`；
- 横屏；
- 适配不同宽高比；
- 适合 Android 和 iOS；
- 使用适当的 Stretch 设置；
- 使用适当的安全区域处理；
- 主场景可直接启动完整 UI 演示。

修改 `project.godot` 时：

- 只修改与本 UI 项目必要相关的配置；
- 不写入无法验证的移动端导出配置；
- 不擅自生成签名或证书；
- 不擅自修改系统级环境；
- 修改后必须重新读取并确认配置结果。

---

## 25. 验收和测试

每次完成 UI 修改后，Codex 必须尽可能执行以下检查：

1. 检查 GDScript 语法错误；
2. 检查场景资源路径；
3. 检查主场景能否启动；
4. 检查控制台是否存在错误；
5. 检查登录流程；
6. 检查加载流程；
7. 检查服务器选择；
8. 检查进入游戏；
9. 检查退出登录；
10. 检查主界面入口；
11. 检查占位页面往返；
12. 检查通用弹窗；
13. 检查按钮正常、按下、禁用和选中状态；
14. 检查不同横屏比例下是否严重错位；
15. 检查地图区域是否保留输入接入条件；
16. 检查 UI 是否意外吞掉整个地图区域的触摸；
17. 检查缺失可选页面时是否能够优雅提示；
18. 检查页面关闭后是否有无效引用或重复信号；
19. 检查任意占位页面移除后其他页面能否继续运行；
20. 汇报无法在当前环境验证的内容。

应创建一个独立的 UI 演示入口或测试场景，用于依次查看：

- 页面；
- 组件；
- 弹窗；
- 资源状态；
- 任务状态；
- 不同按钮状态；
- 加载状态；
- 开发中占位页面。

测试场景不得成为正式页面的强依赖。

---

## 26. 任务完成后的汇报

每次任务完成后，Codex 必须说明：

- 新增了哪些文件；
- 修改了哪些文件；
- 删除了哪些文件；
- 实现了哪些交互；
- 使用了哪些 Mock 数据；
- 是否修改 `project.godot`；
- 是否添加 Autoload；
- 运行了哪些测试；
- 测试结果；
- 尚未完成的部分；
- 无法验证的部分；
- 是否存在需要其他开发者接入的位置。

不得只回复“已完成”。

---

## 27. 冲突处理

当规则冲突时，优先级如下：

1. 用户当前明确指令；
2. 距离目标文件最近的 `AGENTS.md`；
3. 项目根目录的 `AGENTS.md`；
4. 本文件中的具体规则；
5. 现有项目结构和代码惯例；
6. Codex 自己的默认偏好。

若用户指令可能造成数据丢失、覆盖其他开发者工作或破坏非 UI 模块，必须先说明风险并请求确认。

---

## 28. 当前阶段的完成标准

只有同时满足以下条件，才可视为本阶段 UI 框架完成：

- Godot 项目可启动；
- 登录界面可操作；
- Mock 登录可成功和失败；
- 加载界面可正常跳转；
- 开始界面可显示；
- 服务器可以选择；
- 维护服务器不可正常进入；
- 可以进入游戏主界面；
- 可以退出登录；
- 主界面包含资源栏、任务栏和地图占位区；
- 主界面具有设置及其他功能入口；
- 未开发功能可以打开独立占位页；
- 通用弹窗可复用；
- 设置占位页可打开和返回；
- 页面之间无不必要的强耦合；
- 删除任意可选业务页面后其他页面仍能运行；
- 不包含正式地图、战斗、经济和服务器逻辑；
- 不包含网络下载的第三方资源；
- 在多个横屏宽高比下没有严重布局错误；
- 控制台没有由本次实现造成的未处理错误。

---

## 29. MCP 制作与验证工具链

本节已吸收 `MCP_SETUP.md` 的有效内容，是项目 MCP 使用的统一规范。MCP 仅用于辅助制作、检查、运行和验证，不改变本文件规定的模块边界、文件权限和资源来源限制。

### 29.1 工具职责

本项目保留以下工具，职责不得混用：

- 仓库内 `res://addons/godot_ai` 3.0.7：用于 Godot 编辑器内的场景检查、截图和 `McpTestSuite` 测试；
- `@coding-solo/godot-mcp` 0.1.1：用于从 Codex 启动或停止 Godot、读取运行日志和执行项目级场景操作；
- `blender-mcp` 1.6.0，固定 GitHub 提交 `e3ece087`：仅在任务明确需要制作或检查 3D 资产时连接 Blender，核心 HUD 不得依赖它。当前 PyPI 依赖解析缺少 `mcp.server.fastmcp`，因此必须使用该已验证提交。

### 29.2 前置环境

- Godot `4.7.1`；
- Node.js `18+`；
- Blender `3.0+`；
- `uv/uvx`，Blender MCP 固定使用 Python `3.11`。

首次克隆项目后，必须先让 Godot 编辑器完成一次项目导入，再通过 MCP 运行项目，避免全局 GDScript 类缓存尚未生成导致误报。

### 29.3 Codex 配置

- 仓库 `.codex/config.toml` 必须使用可移植命令；
- 若 Windows 桌面启动的 Codex 找不到 `uvx`，只允许在个人 `~/.codex/config.toml` 中将 Blender 的 `command` 改为 `where uvx` 返回的绝对路径；
- 个人绝对路径不得写回仓库配置；
- 同一时间只允许一个 Codex 客户端启动一份 Blender MCP；
- 调用 Blender 场景工具前，必须先在 Blender 的 3D View 侧栏中启用并连接 `Interface: Blender MCP`。

### 29.4 网络、遥测与资源边界

- Blender MCP 默认设置 `DISABLE_TELEMETRY=true`；
- Poly Haven、Sketchfab、Hyper3D、Hunyuan3D 等在线资产或生成入口默认关闭；
- 只有当前任务明确需要、用户已授权且资源来源可确认时，才允许启用在线入口；
- 启用 MCP 不代表获得下载第三方素材、安装插件或修改非任务文件的授权；
- 不得将 API 密钥、个人绝对路径、Blender 偏好文件或 MCP 缓存提交到 Git。

### 29.5 验证要求

使用 MCP 执行制作或检查任务时，应按任务范围尽可能验证：

1. `codex mcp list` 显示 `godot` 和 `blender` 已启用；
2. Godot MCP 返回版本 `4.7.1`，能够运行项目并读取日志；
3. 与本次修改有关的 Godot 运行日志不存在未处理错误；
4. 需要 Blender 时，插件连接后能够读取当前场景信息；
5. Git 变更中不包含 API 密钥、个人绝对路径、Blender 偏好文件或 MCP 缓存；
6. 无法执行的 MCP 或真机验证必须在任务汇报中明确说明，不得用推测代替结果。
