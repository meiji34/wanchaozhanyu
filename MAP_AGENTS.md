# MAP_AGENTS.md

## 1. 适用范围

本文件用于约束 Codex 在 `万朝战域UI设计` Godot 项目中的地图模块开发行为。

- 工作目录：`F:\code\Project\finalsanguo`
- Godot 项目目录：`F:\code\Project\finalsanguo\万朝战域ui设计`
- 地图模块目录：`res://map/`
- 引擎版本：Godot `4.7.1`
- 脚本语言：GDScript
- 目标平台：Android、iOS
- 屏幕方向：横屏
- 地图类型：Grid-based 3D World System（方格 3D 世界系统）
- 表现方案：2.5D 视角，`Sprite3D` + `GLB/GLTF` + 正交 `Camera3D`

根目录 `AGENTS.md` 仍约束项目通用行为和 UI 行为。地图开发任务还必须遵守本文件。若本文件与旧版《地图架构文档.md》中“2D 地图、Node2D、Camera2D”等描述冲突，以本文件为准；后续应另行更新旧接入文档。

---

## 2. Codex 角色

在地图任务中，Codex 扮演 Godot 地图模块开发者，负责：

- 构建可挂载到 UI `SubViewport` 的独立 3D 地图场景；
- 实现地图显示、分区加载、相机拖拽、缩放、旋转和倾斜；
- 实现地图点击、格子拾取、城池显示与选择；
- 实现演示用随机地图生成；
- 维护地图模块与 UI、玩法、服务器之间的清晰边界；
- 编写地图模块的调试能力、验收说明和降级策略。

Codex 不负责：

- 正式战斗系统；
- 正式经济系统；
- 正式服务器通信；
- 正式单位系统；
- 正式寻路系统；
- 正式赛季与联盟逻辑；
- 美术资源生产；
- 未经确认的大范围 UI 架构重构。

---

## 3. 当前阶段目标

本阶段地图需要完成一张可运行的演示地图，范围如下：

- 地图世界有限大，演示规模为 `200 × 200` 格；
- 每个 Chunk 为 `10 × 10` 格；
- 以相机所在 Chunk 为中心，加载两圈 Chunk；
- 地图原点位于整个地图中心；
- 格子坐标使用二维坐标 `Vector2i`；
- 世界坐标使用 Godot 3D 坐标，推荐地面位于 `X-Z` 平面，`Y` 表示高度；
- 地形至少包含山地、河流、平原；
- 地表格、道路、河流使用 `Sprite3D` 或等价的轻量 3D 平面表现；
- 城池、关隘、山体使用 `GLB/GLTF` 转换后的 Godot 场景或占位 `MeshInstance3D`；
- 首版显示 12 座城池；
- 本阶段不生成、不显示正式单位；
- 本阶段不实现寻路和移动范围预览，只预留接口；
- 目标体验为移动端横屏 60 FPS。

---

## 4. 推荐目录结构

地图模块统一放在 `res://map/` 下：

```text
res://map/
├─ map_world.tscn
├─ map_world.gd
├─ controllers/
│  ├─ map_controller.gd
│  ├─ camera_rig.gd
│  └─ map_input_controller.gd
├─ chunks/
│  ├─ map_chunk.tscn
│  └─ map_chunk.gd
├─ tiles/
│  ├─ tile_instance.tscn
│  └─ tile_types.gd
├─ entities/
│  ├─ city_entity.tscn
│  ├─ city_entity.gd
│  ├─ pass_entity.tscn
│  └─ pass_entity.gd
├─ data/
│  ├─ map_generation_config.gd
│  ├─ map_generator.gd
│  ├─ map_data.gd
│  ├─ tile_data.gd
│  └─ city_data.gd
├─ assets/
│  ├─ sprites/
│  ├─ models/
│  └─ placeholders/
└─ debug/
   ├─ map_debug_overlay.gd
   └─ map_debug_draw.gd
```

新增目录和文件应遵守 `snake_case` 命名。脚本类名使用 `PascalCase`。地图 UI 文案和代码注释使用简体中文。

---

## 5. 场景结构

地图主场景建议为：

```text
MapWorld (Node3D)
├─ MapController (Node)
├─ CameraRig (Node3D)
│  └─ Camera3D
├─ ChunkRoot (Node3D)
├─ EntityRoot (Node3D)
├─ SelectionRoot (Node3D)
└─ DebugRoot (Node3D)
```

约束如下：

- `map_world.tscn` 必须可以被独立实例化；
- 地图场景根节点使用 `Node3D`；
- 地图场景由 UI 侧挂载到 `MapArea/MapViewportContainer/MapViewport`；
- 地图模块不得依赖 HUD、登录页、任务栏或弹窗先实例化；
- 地图模块不得直接访问 UI 深层节点；
- 地图释放时必须清理 Chunk、实体、调试节点和信号连接；
- 地图场景缺失或加载失败时，UI 必须保留占位显示，不得崩溃。

---

## 6. 坐标与格子规则

推荐坐标约定：

- 单格尺寸：`2.0` Godot 世界单位；
- 地面平面：`X-Z`；
- 高度方向：`Y`；
- 格子坐标：`Vector2i(x, y)`，其中 `x` 对应世界 `X`，`y` 对应世界 `Z`；
- 地图尺寸：`200 × 200`；
- 地图原点：中心格附近，对应世界坐标 `(0, 0, 0)`；
- 有效格子范围：`x = -100..99`，`y = -100..99`；
- 世界坐标转换应集中在地图坐标工具或 `MapData` 中，不得散落在多个脚本中。

推荐转换公式：

```text
world_x = grid_x * cell_size
world_z = grid_y * cell_size
world_y = height
```

如果后续需要多层地形或复杂高低差，应扩展 `TileData.height`，不要把高度写死到渲染节点中。

---

## 7. 地图数据

本阶段使用局内随机生成的演示地图，不接入服务器地图数据，不要求数据持久化。

推荐数据结构：

- `MapData`：保存地图尺寸、Chunk 尺寸、种子、Tile 数据、城池数据；
- `TileData`：保存格子坐标、地形类型、高度、道路、河流、是否可建城等；
- `CityData`：保存城池 ID、名称、格子坐标、等级、阵营占位字段；
- `MapGenerationConfig`：保存演示地图生成参数；
- `MapGenerator`：根据种子生成地形、河流、道路、城池和关隘。

随机生成原则：

- 优先保证玩法可读性，而不是追求真实地理模拟；
- 河流应连续，避免大量断裂水格；
- 山脉应成带分布，形成视觉和战略阻隔；
- 平原应提供足够可视空间和城池落点；
- 道路应连接主要城池和关隘；
- 城池应放在平原、道路节点或战略交汇处；
- 12 座城池应分散分布，避免全部挤在中心或边缘；
- 生成失败时应退回固定种子或简单默认布局，并输出警告。

后续如果接入正式数据源，应让 `MapData` 成为替换入口，避免重写渲染、相机和拾取逻辑。

---

## 8. Chunk 加载

本阶段使用动态加载与分区管理：

- Chunk 尺寸：`10 × 10` 格；
- 世界总 Chunk 数：`20 × 20`；
- 加载范围：以相机所在 Chunk 为中心加载两圈；
- 推荐最大活跃范围：`5 × 5 = 25` 个 Chunk；
- 相机快速移动时允许分帧生成 Chunk，避免单帧卡顿；
- 离开加载范围的 Chunk 应进入缓存池或释放，首版优先使用缓存池；
- 缓存池大小建议不超过 `36` 个 Chunk；
- Chunk 节点只负责本 Chunk 内地表、装饰和轻量实体的显示；
- 城池等重要实体可以由 `EntityRoot` 统一管理，但必须能随 Chunk 显隐或 LOD 切换。

加载流程建议：

```text
MapController
  -> 读取或生成 MapData
  -> 根据相机位置计算当前 Chunk
  -> 计算目标加载集合
  -> 分帧创建缺失 Chunk
  -> 更新远景低细节表现
  -> 缓存或释放离开的 Chunk
```

---

## 9. 资源与表现

美术资源后续提供。本阶段允许使用程序化占位资源：

- 平原：低饱和绿色或土黄色 `Sprite3D` / 平面网格；
- 河流：蓝绿色 `Sprite3D`，需要连续且方向清晰；
- 道路：土色或浅色 `Sprite3D`，优先连接城池；
- 山地：占位 `MeshInstance3D` 或后续 `GLB` 场景；
- 城池：占位 `MeshInstance3D` 或后续 `GLB` 场景；
- 关隘：占位 `MeshInstance3D` 或后续 `GLB` 场景。

资源规范：

- `GLB/GLTF` 导入后优先转换为 Godot `.tscn` 场景再实例化；
- 禁止在代码中直接依赖外部绝对路径；
- 不从网络下载纹理、模型、字体或插件；
- 缺少正式资源时必须使用占位资源降级，不得中断地图启动；
- 远景不做“虚化”首版实现，改用低细节模型、简化合批图层或降低实体密度；
- 视觉表现必须优先保证读图清晰，不用过强后处理掩盖信息。

---

## 10. 相机与输入

相机结构：

```text
CameraRig (Node3D)
└─ Camera3D
```

相机使用正交投影。推荐默认值：

- `Camera3D.projection = PROJECTION_ORTHOGONAL`；
- 默认正交尺寸：`42.0`；
- 最小正交尺寸：`18.0`；
- 最大正交尺寸：`90.0`；
- 默认俯仰角：`55` 度；
- 俯仰角范围：`35..65` 度；
- 旋转角可变，默认朝向地图北向；
- 拖动阻尼和缩放阻尼应平滑，但不得造成明显延迟。

移动端输入：

- 单指拖动地图；
- 双指缩放；
- 预留旋转视角手势；
- 预留倾斜角变化手势；
- 点击与拖动的判定阈值推荐为 `12` 屏幕像素；
- 长按、双击等操作仅预留，不在首版实现正式行为；
- 相机不得移出地图边界，应根据当前缩放和视口大小限制可移动范围。

桌面调试输入：

- 鼠标左键拖动平移；
- 鼠标滚轮缩放；
- 鼠标中键或右键可用于旋转调试；
- 键盘调试输入必须只在开发调试开关开启时使用；
- 桌面输入不得成为移动端唯一可用路径。

---

## 11. 点击、拾取与选择

点击流程：

```text
屏幕坐标
  -> Camera3D 投射射线
  -> 物理射线或地面平面求交
  -> 世界坐标
  -> 3D 格子坐标换算
  -> Tile ID / Entity ID
  -> 发出中立选择事件
```

拾取优先级从高到低：

1. 单位；
2. 城池；
3. 地图格子。

本阶段不制作单位，但必须保留单位优先级，不要让城池和地表拾取接口阻碍未来单位选择。

选择规则：

- 城池可显示、可点击、可选中；
- 地图格子可点击并返回格子坐标；
- 点击空白区域应取消或更新选择状态，具体行为由地图模块保持一致；
- 地图模块负责显示选中特效，例如格子描边、城池高亮、简单光圈；
- 详情面板、弹窗、城池经营页面由 UI/HUD 或玩法模块负责；
- 地图只发出中立信号，不直接打开具体 UI 页面。

推荐信号：

```gdscript
signal tile_selected(tile_id: Vector2i)
signal city_selected(city_id: String, tile_id: Vector2i)
signal selection_cleared()
```

后续添加单位时可扩展：

```gdscript
signal unit_selected(unit_id: String, tile_id: Vector2i)
```

---

## 12. 模块边界

地图模块负责：

- 地图渲染；
- 地图数据读取或演示生成；
- 坐标转换；
- Chunk 加载、缓存和释放；
- 相机控制；
- 地图输入识别；
- 地表、城池、关隘、山体等可视对象；
- 选中特效；
- 中立选择信号；
- 地图调试显示。

UI 模块负责：

- HUD 布局；
- 地图 `SubViewport` 挂载；
- 地图加载失败时的占位显示；
- `MapUILayer` 中屏幕空间按钮和提示；
- 弹窗、详情面板、页面导航；
- 资源栏、任务栏、功能入口。

玩法和后端模块负责：

- 正式城池归属；
- 正式部队和战斗；
- 行军、寻路、占领；
- 资源、任务、联盟、赛季结算；
- 服务器同步、存档和协议。

地图模块不得为了演示方便把正式玩法状态、服务器协议或经济公式写入地图脚本。

---

## 13. 性能目标

目标设备：

- iOS：最低按 iPhone 15 级别考虑；
- Android：按同等级移动芯片考虑；
- 内存预算：整机内存约 `5 GB` 场景下可运行，地图模块不得假设高端桌面环境。

首版性能目标：

- 移动端横屏目标 `60 FPS`；
- 活跃 Chunk 默认不超过 `25` 个；
- 首次进入地图可有短加载，但不应长时间黑屏；
- 相机移动时 Chunk 创建应尽量分帧，单帧卡顿目标低于 `8 ms`；
- 单次 Chunk 更新建议每帧最多处理 `2..4` 个 Chunk；
- 可见城池数量首版 `12` 座；
- 可见关隘建议 `4..8` 个；
- 同屏装饰物数量应受 LOD 或距离限制控制；
- 远景使用低细节替代，不使用高成本实时虚化。

若实际设备性能不足，应优先降低远景细节、装饰密度、阴影和材质复杂度，而不是牺牲点击准确性和基础读图能力。

---

## 14. 调试工具

地图模块应预留开发期调试开关，发布默认关闭。

建议调试项：

- 显示格子坐标；
- 显示 Chunk 边界；
- 显示当前相机所在 Chunk；
- 显示当前加载 Chunk 数量；
- 显示拾取射线命中点；
- 显示选中的 Tile ID / City ID；
- 显示相机缩放、旋转和俯仰角；
- 显示当前随机种子；
- 输出 Chunk 加载、缓存、释放的简明日志。

调试 UI 不得依赖 HUD 内部节点。可以放在地图模块自己的 `DebugRoot` 或 `MapUILayer` 的独立调试子节点中，但必须保证发布时容易关闭。

---

## 15. 与现有 UI 接入

现有 UI 已提供地图挂载区域：

```text
MapArea
├─ PlaceholderBackground
├─ MapViewportContainer
│  └─ MapViewport
├─ PlaceholderLabel
└─ MapUILayer
```

接入原则：

- 地图主场景挂载到 `MapViewport`；
- 地图场景根节点为 `Node3D` 时，`SubViewport` 可以正常承载 3D 内容；
- `MapUILayer` 只放屏幕空间地图 UI；
- 地图内城池、地表、山体、道路、河流必须在 `MapViewport` 内，由地图相机统一渲染；
- 非交互覆盖层应使用 `MOUSE_FILTER_IGNORE`；
- 交互控件只拦截自身范围，不得用透明全屏控件吞掉地图输入；
- UI 加载地图失败时继续显示占位层，不得中断主界面。

如需调整 `MapArea`、`MapViewportContainer`、`MapViewport` 的节点名称或输入策略，必须同步更新 UI 接入文档和相关脚本。

---

## 16. 编码规范

地图 GDScript 应遵守：

- 使用 Godot `4.7.1` 支持的 API；
- 优先使用静态类型；
- 控制脚本职责单一；
- 地图常量集中管理，不散落魔法数字；
- 资源路径集中定义；
- 节点不存在时判空并降级；
- 可选资源加载失败时输出警告并使用占位资源；
- 不在 `_process()` 中做不必要的全图扫描；
- 信号连接避免重复；
- Chunk、实体和调试节点释放后不得残留引用；
- 注释解释设计原因，不逐行复述代码。

推荐关键常量：

```gdscript
const MAP_SIZE := Vector2i(200, 200)
const CHUNK_SIZE := Vector2i(10, 10)
const CHUNK_LOAD_RADIUS := 2
const CELL_SIZE := 2.0
const CITY_COUNT := 12
const MIN_ORTHO_SIZE := 18.0
const MAX_ORTHO_SIZE := 90.0
const CLICK_DRAG_THRESHOLD := 12.0
```

---

## 17. 验收清单

地图任务完成后，应尽可能验证：

- [ ] `map_world.tscn` 可独立实例化；
- [ ] 地图可挂载到现有 `MapViewport`；
- [ ] 未提供正式美术资源时，占位资源能正常显示；
- [ ] 200×200 演示地图可生成；
- [ ] 平原、山地、河流至少各有可见表现；
- [ ] 12 座城池能显示，并分布合理；
- [ ] 相机单指拖动可用；
- [ ] 双指缩放可用；
- [ ] 桌面鼠标拖动和滚轮缩放可用于调试；
- [ ] 相机不会移出地图边界；
- [ ] 点击和拖动能按阈值区分；
- [ ] 点击城池能选中城池；
- [ ] 点击地表能返回格子坐标；
- [ ] 选中特效可见且不会打开正式 UI；
- [ ] Chunk 能根据相机位置加载和卸载；
- [ ] 活跃 Chunk 数量符合加载两圈的预期；
- [ ] UI 覆盖层不会误吞地图输入；
- [ ] 删除或缺失地图场景时 HUD 能回退到占位显示；
- [ ] 控制台无本次地图实现造成的未处理错误；
- [ ] 无正式战斗、经济、服务器、单位或寻路逻辑混入地图模块。

---

## 18. 任务完成汇报

地图任务完成后，Codex 必须说明：

- 新增了哪些地图文件；
- 修改了哪些 UI 接入文件；
- 是否修改 `project.godot`；
- 是否添加 Autoload；
- 使用了哪些占位资源；
- 使用了哪些演示数据或随机生成规则；
- 实现了哪些交互；
- Chunk 加载策略和当前参数；
- 运行了哪些检查；
- 无法验证的移动端性能风险；
- 尚未实现但已预留的接口；
- 是否需要 UI、玩法或美术开发者继续接入。

不得只回复“已完成”。
